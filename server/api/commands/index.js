const push = require('@server/api/commands/push')
const update = require('@server/api/commands/update')
const list = require('@server/api/commands/list')
const remove = require('@server/api/commands/remove')

module.exports = {
  push,
  update,
  list,
  remove
}
