const push = require('@/api/commands/push')
const update = require('@/api/commands/update')
const list = require('@/api/commands/list')
const remove = require('@/api/commands/remove')

module.exports = {
  push,
  update,
  list,
  remove
}
