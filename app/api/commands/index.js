const push = require('@app/api/commands/push')
const update = require('@app/api/commands/update')
const list = require('@app/api/commands/list')
const remove = require('@app/api/commands/remove')

module.exports = {
  push,
  update,
  list,
  remove
}
